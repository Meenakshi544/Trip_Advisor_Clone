<%= form_with model: [@hotel, review, review.usercomments.build ] do |form| %>
  <div>
    <%= form.text_area :body, :rows=>2, :cols=>150,:placeholder=>"Enter a comment on the above review" %>
  </div>
  <div class="my-2">
    <%= form.submit %>
  </div>
<% end %>